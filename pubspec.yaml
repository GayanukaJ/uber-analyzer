name: uber_analyzer
description: Uber trip notification analyzer

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_notification_listener: ^1.4.0
  flutter_overlay_window: ^0.5.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  flutter_background_service: ^5.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
